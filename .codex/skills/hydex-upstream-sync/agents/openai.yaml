interface:
  display_name: "Hydex Upstream Sync"
  short_description: "Replay, publish, and finalize Hydex refs"
  default_prompt: "Use $hydex-upstream-sync to replay hydex/main when its OpenAI base advances and, when authorized, publish atomically, align local refs, preserve provenance, and clean up superseded worktrees."
