interface:
  display_name: "Hydex Upstream Sync"
  short_description: "Replay Hydex onto an exact OpenAI base"
  default_prompt: "Use $hydex-upstream-sync to replay hydex/main only when the plugin Codex base advances, then refresh the VSIX and conditionally rebuild system packages."
